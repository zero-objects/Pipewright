# catalog/classify/aws_codebuild.toml — construct classification,
# derived from ir.toml: an entry with one of these keys has
# a value-mapping of the named IR construct.

[meta]
platform = "aws_codebuild"
key_count = 2

[classify]
artifacts = "artifact"
cache = "cache"
