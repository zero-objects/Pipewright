//! Construct classification for jenkins. Generated.

/// `(key, ir-construct)`.
pub const CONSTRUCT_KEYS: &[(&str, &str)] = &[
    ("agent", "agent"),
    ("matrix", "matrix"),
    ("parameters", "parameter"),
    ("retry", "retry"),
    ("stages", "stage"),
    ("steps", "step"),
    ("triggers", "trigger"),
];

/// Construct-field keys whose value is canonically a LIST
/// (the single-mapping form is sugar for a one-item list).
pub const LIST_CONSTRUCT_KEYS: &[&str] = &["parallel", "parameters", "stages", "steps"];
